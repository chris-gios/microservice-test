server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/chris-gios/microservice-test
          search-paths: config-data
  application:
    name: config-service